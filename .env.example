# AzerothCore MCP Server Configuration
# Copy this file to .env and update with your database credentials
#
# Setup:
#   1. sudo apt install python3.13-venv
#   2. python3 -m venv venv
#   3. ./venv/bin/pip install -r requirements.txt
#   4. Copy this to .env and edit credentials
#   5. Add to ~/.claude/settings.json:
#
#      {
#        "mcpServers": {
#          "azerothcore": {
#            "command": "/path/to/azerothMCP/venv/bin/python3",
#            "args": ["/path/to/azerothMCP/server.py"],
#            "env": {
#              "DB_HOST": "localhost",
#              "DB_PORT": "3306",
#              "DB_USER": "acore",
#              "DB_PASSWORD": "acore"
#            }
#          }
#        }
#      }
#
#   6. Restart Claude Code

# MySQL Connection Settings
DB_HOST=localhost
DB_PORT=3306
DB_USER=acore
DB_PASSWORD=acore

# Database Names (update if different from defaults)
DB_WORLD=acore_world
DB_CHARACTERS=acore_characters
DB_AUTH=acore_auth

# Read-only mode (default: true)
# Set to "false" to allow INSERT, UPDATE, DELETE queries
READ_ONLY=true

# Enable spell_dbc tool (default: false)
# Only needed if you have custom spells
ENABLE_SPELL_DBC=false

# Enable wiki search tools (default: false)
# Disabled by default to reduce token usage - AI often searches wiki unnecessarily
# Set to "true" to enable wiki search and read_wiki_page tools
ENABLE_WIKI=false

# Enable source code search tools (default: false)
# Disabled by default to reduce token usage - AI often searches source unnecessarily
# Set to "true" to enable search_azerothcore_source and read_source_file tools
ENABLE_SOURCE_CODE=false

# Wiki path (only needed if ENABLE_WIKI=true)
WIKI_PATH=/home/arthas/wiki/docs

# AzerothCore source path (only needed if ENABLE_SOURCE_CODE=true)
# Set this to your AzerothCore repository to enable source code tools
AZEROTHCORE_SRC_PATH=/home/arthas/azerothcore

# SOAP Configuration (for live worldserver commands)
# Requires SOAP.Enabled = 1 in worldserver.conf
# Account must have SEC_ADMINISTRATOR (gmlevel 3+)
SOAP_ENABLED=false
SOAP_HOST=127.0.0.1
SOAP_PORT=7878
SOAP_USERNAME=
SOAP_PASSWORD=
